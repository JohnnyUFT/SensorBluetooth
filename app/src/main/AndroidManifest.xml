<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br.sensor" >
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Sensor de Umidade"
        android:theme="@style/AppTheme"> <!-- android:theme="@style/AppTheme" -->
        <activity
            android:name="com.br.sensor.MainActivity"
            android:label="Sensor" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.br.sensor.ListaDispositivos"
            android:theme="@android:style/Theme.Dialog"
            android:label="Dispositivos Pareados">
        </activity> <!-- toda esta activity foi incorporada manualmente -->
    </application>

</manifest>
